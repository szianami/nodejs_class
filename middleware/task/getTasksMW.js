/**
 * Load all task from the database
 * The result is saved to res.locals.tasks
 */
 const requireOption = require('../requireOption');

 module.exports = function (objectrepository) {
     return function (req, res, next) {
         next();
     };
 };